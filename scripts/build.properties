#names
project.name=Gradiel
omniel.name=Omniel
harachel.name=Harachel
project.name.dev=gradiel
omniel.name.dev=omniel
harachel.name.dev=harachel

#site
project.site=http://www.omniel.net/gradiel.shtml

#version
project.version=01
omniel.version=094
harachel.version=012

project.name.versioned=${project.name.dev}-${project.version}
omniel.name.versioned=${omniel.name.dev}-${omniel.version}
harachel.name.versioned=${harachel.name.dev}-${harachel.version}

#build locations
asdoc.loc=${FLEX_HOME}/bin/asdoc
main.src.loc=${basedir}/src
test.src.loc=${basedir}/test
doc.loc=${basedir}/doc
lib.loc=${basedir}/libs
bin.loc=${basedir}/bin
report.loc=${basedir}/report
dist.loc=${basedir}/dist
template.loc=${basedir}/build/templates

#project files
readme=README.textile
license=LICENSE
changelog=CHANGELOG.textile